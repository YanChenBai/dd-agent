[package]
name = "native_win32"
version = "0.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = "3.0.0"
napi-derive = "3.0.0"

[target.'cfg(windows)'.dependencies]
cpal = "0.17.1"

[build-dependencies]
napi-build = "2"

[profile.release]
lto = true
strip = "symbols"
